import { Identifier } from '../types/global';

const USER_ROLES: Identifier[] = [
  { name: 'user.role.observer', id: 'observer' },
  { name: 'user.role.backend', id: 'backend' },
  { name: 'user.role.frontend', id: 'frontend' },
  { name: 'user.role.qa', id: 'qa' },
];

export default USER_ROLES;
