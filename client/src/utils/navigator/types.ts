export enum OSType {
  Linux = 'linux',
  Mac = 'mac',
  Windows = 'windows',
}

export enum BrowserType {
  Chrome = 'chrome',
  Edge = 'edge',
  Firefox = 'firefox',
  IE = 'ie',
  Safari = 'safari',
}
